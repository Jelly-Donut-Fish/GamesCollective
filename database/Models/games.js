const { pool } = require('../index');

const getAll = function() get{
 //TODO: write query to get all games form db
}

const getOne = function(game_id) get{
 //TODO: write query to single game form db
}

module.exports = {
  getAll: getAll,
  getOne: getOne
}